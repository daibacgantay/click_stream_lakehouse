# Trino chạy single-node: coordinator đồng thời là worker
# Phù hợp local dev — production tách riêng coordinator và nhiều worker
coordinator=true
node-scheduler.include-coordinator=true
http-server.http.port=8080
discovery.uri=http://localhost:8080
